[build-system]
requires = [
    "scikit-build-core",
    "numpy >= 1.20.0"
]
build-backend = "scikit_build_core.build"

[project]
name = "rstatspy"  # as it would appear on PyPI
version = "0.0.0"
readme = "README.md"

[tool.scikit-build]
build.targets = ["rstatspy"]